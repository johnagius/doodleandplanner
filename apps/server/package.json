{
  "name": "@dap/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@dap/shared": "*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241112.0",
    "vitest": "^2.1.4"
  }
}
